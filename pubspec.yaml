name: frame
description: A map editor for Pokémon GBA games

publish_to: 'none'

version: 0.1.0

environment:
  sdk: ">=2.12.0-214.0.dev <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  pedantic: ^1.9.2
  path: ^1.7.0
  cupertino_icons: ^1.0.0
  nativeshell:
    version: ^0.1.9
  provider: ^6.0.2
  file_picker: ^4.4.0
  split_view: ^3.1.0
  tabbed_view: ^1.10.0+1
  async_builder: ^1.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
